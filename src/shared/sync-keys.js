/**
 * Re-export para el bundle del renderer (webpack).
 * Fuente canónica: src/main/utils/sync-keys.js (incluida en electron-builder via src/main/**).
 */
module.exports = require('../main/utils/sync-keys');
