import React from 'react';

/**
 * Componente que proyecta las líneas guía holográficas en tiempo real
 * cuando un panel se alinea magnéticamente a bordes, vecinos o alturas compartidas.
 */
const HomePanelGuideOverlay = () => {
  return null;
};

export default React.memo(HomePanelGuideOverlay);

